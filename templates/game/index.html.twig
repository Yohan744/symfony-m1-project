{% extends 'base.html.twig' %}

{% block title %}
	Mon village
{% endblock %}

{% block body %}

	<div class="layout-bg">

		<img src="{{ asset(theme.image) }}" alt="background">

		{{include("components/sidebar.html.twig") }}

		<div class="content">
			{{component('buildingCard', {
					building: user.townhall
				})}}

			{% for building in userBuildings %}
				{{component('buildingCard', {
					building: building
				})}}
			{% endfor %}
		</div>

	</div>
{% endblock %}
